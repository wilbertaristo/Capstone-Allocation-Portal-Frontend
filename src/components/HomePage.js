import React from "react";
import { Form, Button, Divider, Alert } from 'antd';

function HomePage(){

    return(
        <div>
            <h1>Home Page</h1>
        </div>
    );
}

export default HomePage;